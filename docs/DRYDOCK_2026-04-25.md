# Drydock 2026-04-25 (Saturday) — Full Session Record

**Session duration:** ~8 hours
**Gate state:** `_EXECUTION_ENABLED=False` throughout
**Alpaca paper account:** untouched (~$79,716 equity at session start)
**Bigmac RAM:** peaked clean; Ollie Box absorbed all heavy model traffic

---

## All 14 Fixes (by file:line)

### FIX 1 — is_halted gate in buy()
**File:** `engine/paper_trader.py:~542`
**Before:** buy() checked `_is_human_player()` then proceeded; `is_active` was decorative
**After:** SQL gate added immediately after human check:
```python
_halt = _conn().execute("SELECT is_halted, halt_reason FROM ai_players WHERE id=?", (player_id,)).fetchone()
if _halt and _halt[0]:
    _last_rejection[player_id] = f"Halted: {_halt[1] or 'no reason given'}"
    return None
```
**Smoke test:** `buy('grok-3',...)` returns None while `is_halted=1` ✅

### FIX 2 — is_halted gate in sell()
**File:** `engine/paper_trader.py:~1083`
**Before/After:** identical pattern to FIX 1, applied to sell()

### FIX 3 — Zombie players halted in DB
**File:** `data/trader.db` — `ai_players` table
**Before:** `ollama-llama`, `grok-3` had `is_active=0` (decorative); still routed through execution
**After:** `is_halted=1`, `halt_reason='S6 review: routing zombie, retired 2026-04-25'`

### FIX 4 — Autopilot phantom $0 P&L — Layer 1 (skip-when-no-price)
**File:** `engine/autopilot.py:126`
**Before:** `current_price = prices.get(sym, {}).get("price", pos["avg_price"])` — fell back to avg_price, generating RSI trim at entry price = $0 realized P&L
**After:** `current_price = prices.get(sym, {}).get("price", 0)` + `if current_price <= 0: continue`

### FIX 5 — Autopilot phantom $0 P&L — Layer 2 (widen prices dict)
**File:** `main.py:~1095` in `run_autopilot()` caller
**Before:** only iterated `WATCH_STOCKS` (20 symbols) to build prices dict
**After:** queries `SELECT DISTINCT symbol FROM positions WHERE qty > 0`, adds all to `syms_to_fetch`

### FIX 6 — `_try_db_fallback` returning option premium as stock price
**File:** `engine/market_data.py:~487`
**Before:** `SELECT price FROM trades WHERE symbol=?` — matched option trades for MU, returned $42.48 (call premium) as MU stock price
**After:** `SELECT price FROM trades WHERE symbol=? AND asset_type='stock'` (both queries in the function)

### FIX 7 — BUY_CALL fail-closed when no option data
**File:** `engine/paper_trader.py:~1364`
**Before:** `buy_price = price` (stock price used as option premium fallback if selector fails)
**After:** `buy_price = None`; guard added: `if expiry_date is None or buy_price is None: return None`

### FIX 8 — Capitol Trades triple-entry dedup
**File:** `engine/crew_scanner.py:~2569` in `_process_rules_player()`
**Root cause:** Two independent code paths buy for `capitol-trades`:
  1. `capitol_fund.py::run_capitol_scan()` — has `_done_today`, `held_symbols`, `_already_bought_today()` — fires 7:35–8:30 AZ only
  2. `crew_scanner.py::capitol_rules()` via `_process_rules_player()` — zero dedup, fires every 2 min
**Evidence:** FMAO bought 3x at 15:21/15:28/15:31 UTC Apr 24 at $26.52 (21.14 shares total). Reasoning format `"Capitol Trades: Robert E. Latta -> FMAO [FRESH_24H] score=72"` confirms crew_scanner path.
**After:** Guard inserted before `buy()` call — checks existing portfolio position AND `action LIKE 'BUY%'` DB query (same-day). `DEDUP_HELD` and `DEDUP_TODAY` gate results logged.
**Widening:** `action.startswith("BUY")` + `action LIKE 'BUY%'` catches BUY_CALL etc.

### FIX 9 — NTFY topic: riker_synthesis + fleet_auditor + alert_channels
**Files:** `engine/riker_synthesis.py:31`, `engine/fleet_auditor.py:29`, `engine/alert_channels.py:41`
**Before:** `"Ollie-Alert-35"` **After:** `"ollietrades-admin"`

### FIX 10 — NTFY topic: ntfy.py
**File:** `engine/ntfy.py:4,15`
**Before:** `"Ollie-Alert-55"` **After:** `"ollietrades-crew"`

### FIX 11 — Riker notification title
**File:** `engine/riker_synthesis.py:197`
**Before:** `_ntfy("Riker Fleet Alert", ...)` **After:** `_ntfy("Fleet Alert", ...)`

### FIX 12 — ntfy tags rename
**File:** `engine/alert_channels.py` (6 occurrences)
**Before:** `"trademinds"` **After:** `"ollietrades"` (function default, dict values, test call)

### FIX 13 — BSM ceiling (Layer A, Phase 2)
**File:** `engine/options_selector.py`
**Added:** `_bsm_call()`, `_bsm_put()` using `math.erf` (stdlib only, no scipy)
**Constants:** `_BSM_RISK_FREE = 0.045`, `_BSM_CEILING = 1.5`
**Logic:** after strike selection, if `option_premium > 1.5 x BSM_fair_value` -> return None
**Captures:** `best_iv` inside selection loop; uses `0.45` fallback if chain IV unavailable
**Smoke test:** MU 500C 27DTE BSM fair $12.45 — Spock paid $42.48 (3.4x fair) — blocked ✅
**SPY 670P 30DTE IV=0.20:** BSM fair $4.48 — in expected range ✅

### FIX 14 — Earnings blackout (Layer B, Phase 2)
**File:** `engine/options_selector.py`
**Replaced:** dead logs-only block (lines 150-175 pre-patch) that set `earnings_warning` but never blocked
**Added:**
- `_next_earnings_date(symbol)` — 3-tier lookup: in-memory TTL cache (6h) -> `data/earnings_cache.json` (1ms, 42 symbols) -> yfinance fallback -> fail open
- Blackout guard: `0 <= days_to_earnings <= 3` (pre-earnings IV expansion) OR `abs(days_earnings_to_expiry) <= 5` (event straddles expiry)
- Warning tier preserved: 4-7 days sets `earnings_warning=True` in result but does not block
**Cache timing:** first call 1.0ms (file), second call 0.0ms (mem) ✅

---

## Smoke Test Results

| Test | Result |
|---|---|
| `buy('grok-3',...)` while halted -> None | ✅ |
| `_bsm_call(471,500,27/365,0.045,0.45)` -> $12.45 | ✅ |
| `_bsm_put(700,670,30/365,0.045,0.20)` -> $4.48 | ✅ |
| `_next_earnings_date('NVDA')` -> 2026-05-20 (1ms file) | ✅ |
| `_next_earnings_date('NVDA')` second call (0ms mem) | ✅ |
| All 9 module imports clean | ✅ |
| `_EXECUTION_ENABLED = False` | ✅ |
| Bigmac Ollama: nothing loaded | ✅ |
| Capitol DEDUP_HELD fires for CBRL (known held) | ✅ |

---

## Key Audit Finding

`is_active`, `is_paused`, and `crew_role` are **decorative** in the execution path — checked only for display/UI purposes. The execution chain (`buy()` -> `sell()` -> `execute_signal()`) never checked them. **`is_halted` is the only working per-player kill switch.** Document this before wiring any new agent.

---

## Tuesday Gate-Flip Checklist

Before flipping `_EXECUTION_ENABLED = True`:
- [ ] iv_history Day 4 verified clean (Mon Apr 28 @ 9:45 MST)
- [ ] iv_history Day 5 verified clean (Tue Apr 29 @ 9:45 MST)
- [ ] Ghost scorecard reviewed via `/api/signals/scorecard`
- [ ] `/api/wheel/status` 500 root-caused or confirmed non-blocking
- [ ] FMAO position reviewed — 21.14 shares at $26.52 still open, stop at $23.34
- [ ] Spock MU positions reviewed — stock 0.89sh @ $471.40 + 500C 27DTE 11.51 contracts @ $42.48

---

## Open Items (carry forward)

- `/api/wheel/status` intermittent 500 (`dashboard/app.py:7592`) — low priority, does not block trading
- Ghost scorecard calibration before gate-flip
- Chekov rehab: `git show 859a4f0:engine/chekov_autotrade.py` S5 ghost-trade
- Polygon.io Options Starter ($29/mo) — approved in principle, not activated
- Spock MU positions still open: stock 0.89sh @ $471.40 + 500C 27DTE 11.51 contracts @ $42.48 — review Monday alongside FMAO 21.14sh @ $26.52
