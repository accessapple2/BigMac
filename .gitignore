# Secrets
.env
.env.save
.env.bak
.env.backup
.env.old
*.env
.env.beforeDBpathFix.backup
.env.*.backup

# Virtual environments
venv/
venv_py312_archive/
.venv/
.venv-crew/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Data / DB (runtime state, not source)
data/*.db
data/*.db-shm
data/*.db-wal
data/.force_scan
data/*.json
data/*_cache.json
data/backtest_cache/
# Strategy lab auto-optimizer output (regenerated per run)
data/strategy_lab/
data/.fuse_hidden*
data/s6_180d_backtest.json
data/s6_sim_180d_backtest.json
data/holly_nightly_*.txt
data/danelfin_scores.json
logs/

# Migration artifacts (pre-cutover DB snapshots)
*.preMigration.archive

# Defensive backups created before major operations
# Convention: <file>.pre_<operation>_<timestamp>
*.pre_sprint_*
*.pre_rotation_*
*.pre_migration_*

# macOS
.DS_Store
**/.DS_Store

# Claude Code local settings
.claude/settings.local.json

# Jupyter
.ipynb_checkpoints/

# Misc
*.log
nohup.out
lib/TradingAgents/
# Large database files
backups/*.db
backups/*.db-shm
backups/*.db-wal
signal-center/signals.db
signal-center/*.db

# Virtual environments
.venv-crew/
scrapling-venv/
venv/

# Large databases
signal-center/signals.db
data/trader.db
backups/*.db
backups/*.db-shm
backups/*.db-wal

# Logs
logs/*.log

# Temp files
*.db-shm
*.db-wal
cookies.txt
trading.db
autonomous_trader.db
# Root-level legacy DB stubs (absent from working tree, untracked for hygiene)
arena.db
scanner.db
# UOA database at repo root (distinct from data/trader.db main DB)
trader.db

# Manual UI backups (use git history instead)
dashboard/static/*.bak

# Generated assets (rebuilt each morning run)
dashboard/static/briefing_audio.mp3

# Python/library artifacts
did.bin

# Dated backup files (.bak-2026-04-19, .bak_152251 styles)
*.bak-*
*.bak_*

# Pre-op snapshots (.before_fix_20260418, .before_port8000_fix style)
*.before_fix_*
*.before_port*

# Local backup archive directory (relocate outside repo)
backups_archive/
backups/archive-*/
data/real_holdings.json
data/metals.json

# Dated .env backups with dot-suffix (e.g. .env.bak.2026-04-20-alpaca)
.env.bak.*

# Trader DB stub archives
trader.db.stub-archive-*

# Gitignore backup files
.gitignore.before_*

# Decommissioned backup subdirs
backups/decommissioned-*/

# Pre-unpause operation snapshots
*_pre_unpause_*.txt

# ── HM-G additions (2026-05-04) — fat-file prevention ─────────────────────────
# Audit lesson: 1.32 GB of DB backup snapshots landed in commit 463c402 and
# blocked origin push for a week. Never again.

# Database backup snapshots in backups/ — catches trader.db.pre_*, .pre-*, etc.
backups/*.db.*

# DB working sidecars beside backup snapshots
backups/*-shm
backups/*-wal

# Stub DB at repo root (distinct from data/trader.db)
/trader.db

# Deprecated / archive markers (e.g., arena.db.deprecated_20260503_182837)
*.deprecated_*

# FUSE archive directories
.fuse_archive_*/

# Editor crash/merge backups (narrow — does NOT match the existing
# dashboard/app.py.bak.* sprint-backup convention which is intentionally tracked)
*.orig
*.swp

# ── HM-W additions (2026-05-05) — chronic working-tree noise ─────────────────
# Runtime artifacts regenerated by the service. Were tracked previously and
# kept showing as modified each session. Untrack via git rm --cached + ignore.
dashboard/static/morning_briefing.mp3
data/equity_curve.json
data/premarket_gaps.json

# Schwab CSV import archive — local-only audit trail, not source of truth.
data/schwab_csv_archive/

# Schwab inbox — transient personal CSVs dropped by Admiral, never commit (HM-AT-β 2026-05-07).
inbox/*
!inbox/.gitkeep

# Top-level XO_BACKLOG snapshots (use git history; live file is source of truth).
docs/*.bak
docs/*.bak.*

# HM-I-β-Item5: daily reconciliation reports — generated daily, not source-controlled
data/reconciliation/
